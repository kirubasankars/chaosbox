version("1.0.0");
description("Redis backend for chaosbox counter demos");
selectors("worker");
resources {
  memory { min("64 mb"); max("256 mb"); };
  cpu { min("100 mhz"); max("500 mhz"); };
  ports {
    chaosbox_redis_port { };
  };
};
